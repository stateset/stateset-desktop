# StateSet Desktop v1.0.1 Release Notes

## What's new

- Bumped app version metadata to `1.0.1` in package metadata.
- Updated in-app version fallback strings to match the `1.0.1` release.
- Updated local test fixture version reference to `1.0.1-test`.

## Build / release notes

- This release prepares versioning consistency for the public repository.
- For this tag, release artifacts were built in CI but attachment to the GitHub release must be verified in the release workflow (ensure build jobs completed and artifact upload paths matched).

## Notes

- No functional runtime behavior changes were introduced in this release.
